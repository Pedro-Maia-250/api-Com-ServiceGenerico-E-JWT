package com.lunarvoid.repositories;

import org.springframework.data.jpa.repository.JpaRepository;

import com.lunarvoid.entities.Retangulo;

public interface RetanguloRepository extends JpaRepository<Retangulo,Long> {
    
}
